
# a voir les structure de fichier dans le fichier readme 
pour afin de les cree pour que le program se lance 

# se fichier se trouve dans le docier use_case

from interface_adapter.b import B 

class C :
    def get2(self):
        b = B()
        c = str (b.get1())
        return c 
